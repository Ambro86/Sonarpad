# 更新日志

版本 0.9.11 – 2026-09-22

文件保存
1. 修复“另存为”：当一个已经从磁盘打开的文档以新名称保存时，保存对话框现在会从原文件所在的同一文件夹打开，而不是从已配置的默认文件夹或 OneDrive 开始。新建文档仍继续使用已配置的保存文件夹。

Google 语音
1. 为 Google 语音启动加入了安全回退机制：如果 Chrome 在 Google TTS 运行环境准备完成前退出，Sonarpad 会在 Microsoft Edge 可用时自动改用 Edge 重试。对于 Google 语音本来就能正常工作的电脑，现有的 Chrome 启动流程保持不变。

2. 改进了 Google 语音所用浏览器在少数启动失败情况下的诊断：只有当 Chrome 或 Edge 在 Google TTS 运行时准备完成前退出时，才会把 stdout 和 stderr 写入 Sonarpad 诊断。正常启动不会把浏览器输出加入诊断。

版本 0.9.10 – 2026-09-16

音频描述
1. 改进音频描述：将字符 `_`（下划线）替换为空格，避免语音合成在音频描述中朗读该字符。

版本 0.9.9 – 2026-09-15

AI 音频描述
1. 为没有音轨的源视频加入了隔离式回退机制，不改变已经正常工作的现有流程。Sonarpad 仍会先执行正常导出；只有当 FFmpeg 报告缺少音频流，并且 Sonarpad 确认源文件确实没有任何音轨时，才会创建与视频等长的静音源，并将合成的描述混入其中。若导出到原始视频，则继续复用现有的视频+音频封装路径。原本含有音频的视频仍完全走原有正常流程。

2. 扩展了“无可用描述”情况下严格隔离的备用方案，同时不修改正常的音频描述流程。Sonarpad 仍然始终先运行现有分析；只有在没有可用描述时，才提供第二次“简短”重试，该重试使用完全相同的流程，并继续遵守所有静音和不得覆盖对白的限制。如果第二次尝试仍无法放置描述，Sonarpad 现在会明确询问是否允许最后一次备用处理。只有用户选择“是”后，才会复用第二次尝试中 Gemini 已生成并保存到 checkpoint 的简短描述，并在对应的视觉时间点以 ducking 方式混音，允许与对白短暂重叠。Pyannote、Gemini bridge、正常对齐规则、TTS 调度以及前两条分析路径均不会被修改。如果用户拒绝，或没有可复用的已生成描述，Sonarpad 会以本地化消息正常结束。

3. 在不改变正常音频描述流程的前提下，进一步优化了“无可用描述”时的独立回退逻辑。如果用户最初选择的详细程度已经是“简短”，而分析结束后仍没有可用描述，Sonarpad 现在会跳过多余的第二次 Gemini 分析；当存在可复用的已生成描述时，会直接询问是否使用最终的对白重叠回退。如果最初选择的是“标准”或“详细”，即使可用停顿很短，也仍会保留“简短”重试：此时它还用于为可能的最终回退生成更短的描述，从而减少旁白覆盖对白的时间。Pyannote、Gemini bridge 以及正常的静音和时间对齐规则均保持不变。

播客录制
1. 为部分有问题的 WASAPI 麦克风驱动加入了保守的设备位置回退机制，不影响原本正常工作的录音流程。仅在设备位置持续不匹配时启用：连续 24 次不匹配，或在 64 个正常数据包中至少 80% 不匹配。真正的 WASAPI `DATA_DISCONTINUITY` 标志和时间戳错误仍保持最高优先级，系统音频捕获不受影响。

YouTube 和流媒体
1. 修复了从结果上下文菜单启动时失败的 YouTube 下载。对于仅频道会员可用的视频，已知的 yt-dlp 错误现在会转换为 Sonarpad 的本地化提示，而不再直接显示原始英文错误。关闭错误后，Sonarpad 会等原生上下文菜单/模态循环真正结束后再恢复焦点，从而可靠地返回结果列表并保持键盘可用。正常成功的下载流程保持不变。

2. 预防性筛选已与移动版 SonarTube 对齐：在搜索结果以及浏览频道/播放列表时，如果 YouTube/yt-dlp 完全没有返回观看次数数据，对应视频将被隐藏。频道和播放列表仍会显示，真正为 0 次观看的视频也会保留。仅限频道会员内容的本地化错误处理仍作为第二层后备机制。播放列表批量下载保持不变。

版本 0.9.8 – 2026-09-12

AI 音频描述
1. 新增保守的多声道导出恢复机制，不改变已经正常工作的文件流程。Sonarpad 仍会先使用原始声道布局，仅在必要时补齐最后一个不完整的 PCM 帧。如果多声道中间 WAV 仍因样本数不是声道数的整数倍而失败，Sonarpad 只会自动将这一次导出以立体声重试。原本正常的单声道、立体声和多声道导出保持不变。

深色主题与无障碍
1. 修复深色主题干扰 Windows 原生对话框和确认消息的问题。Sonarpad 不再对系统 `#32770` 对话框树应用自定义深色主题 subclass，因此“打开/保存”、诊断导出和 MessageBox 继续由 Windows 管理，并可正确获得键盘/NVDA 焦点。同时修复了诊断“保存”对话框中默认 ZIP 扩展名的内存生命周期。

版本 0.9.7 – 2026-09-11

AI 音频描述
1. 新增可选的“在原始视频文件中创建音频描述”功能。启用后，Sonarpad 现在优先输出 MP4：原始视频流会在不重新编码的情况下复制，并加入混合后的音频描述轨道。如果 FFmpeg 在创建 MP4 时报告容器或数据包写入兼容性错误，Sonarpad 会自动将同一次导出改为 MKV 重试，视频仍不会重新编码。用户也可以直接选择 MKV。此模式会忽略扩展停顿，以保持音频与视频同步。

2. 改进了已保存音频描述项目中的片段重新分析。该窗口现在直接使用主“创建音频描述”窗口中已经配置好的 AI 访问方式，因此不再重复显示 API 密钥、Sonarpad AI 余额和模型选择。重新分析一个片段后，现在会保留属于该分析片段的整组项目描述，而不是只保留第一条或时间最近的一条；组内所有描述都会标记为“已重新分析”，并且“应用片段并重新导出 MP3”会一次性应用整组内容。扩展停顿的预览现在直接进行语音合成，不再在已导出的 MP3 中跳转定位，从而避免预览只播放电影原声或截断描述。

3. 在不改变现有正常流程的前提下，改进了 Gemini 媒体的保守回退机制。HTTP 400 `INVALID_ARGUMENT` 仍会先触发较小的 MKV 分段（约 15 MB），再使用兼容的 MP4 分段。另外，如果 Gemini 已接受上传的分段，但处理最终进入 `FAILED` 状态，Sonarpad 会先原样重新上传该分段一次，仍失败才转入 MP4 回退；服务器 Code 13 的重试限制为三次，之后使用同一回退路径，避免无限等待。网络、配额、身份验证或语音合成错误不会触发此路径。

4. 修复了切换 AI 访问模式时凭据的保存问题。个人 Gemini API 密钥和 Sonarpad AI 访问代码现在会独立保存：在“个人 API 密钥”和“Sonarpad AI”之间切换时，不再清除未激活模式的凭据。Gemini 密钥输入框失去焦点时也会立即保存。

5. 为“重新分析片段”和“应用片段并重新导出 MP3”新增了真正的“取消”按钮。进度条仍会正常显示，而“取消”现在会在当前阶段能够安全停止时，中止 FFmpeg 片段准备、AI 工作进程、TTS 校验以及最终导出。重新分析片段的应用现在采用事务式处理：只有重新导出成功后才会替换现有项目和输出媒体文件；如果取消，原有文件保持不变，重新分析得到的提案也会保留以便再次尝试。

6. 修复了首个 Gemini 分块之后的描述无法重新分析的问题。现在，单独分块会以从 0 开始的本地时间轴发送给现有 worker，返回后再映射回项目中的绝对位置，从而避免 `Invalid prepared chunk timeline at chunk 1`，且不改变正常的音频描述流程。

7. 已重新设计已保存项目中的片段重新分析，使其使用与完整生成音频描述相同的安全检查。所选片段现在会经过相同的 16 kHz 单声道音频提取和 Pyannote 对话/静音分析、相同的 Gemini 时间规则与媒体回退、使用项目语音的相同真实 TTS 合成，以及用于安全放置和扩展停顿的相同最终调度器。应用片段时，Sonarpad 会保留新的已验证绝对时间，并更新该片段的 Pyannote 保护区间；此前针对过长重新分析文本的特殊预览变通方案已移除。

8. 修复了完整重新分析后的片段结构替换。Sonarpad 不再要求重新验证后的片段必须与已保存项目中的描述数量完全一致：该 chunk 中的旧描述会由完整流程生成并验证通过的全部安全结果替换，因此原有 10 条描述的片段可以正确变为 9 条（或 11 条）。项目编辑器会立即显示新的数量和时间以供预览，而已保存的项目和媒体文件只有在“应用片段并重新导出 MP3”成功完成后才会被修改。

9. 重新设计了片段重新分析：所选的物理片段现在会被当作一个真正独立的迷你影片处理。Sonarpad 会把这个迷你影片直接交给普通完整创建时使用的同一个 `create_audio_description` 函数，因此视频和音轨共享完全相同的本地时间线，并直接执行正常的 Pyannote、Gemini、真实 TTS、调度与安全检查，不再维护另一套重新分析流程。只有在正常流程全部完成后，结果中的本地时间才会加回偏移量，映射到原影片中的位置。

文本编辑
1. 改进了“编辑 > 文本”中的“合并自动换行”。未选择文本时处理整个文档；有选择时只处理所选行。现在即使上一句已经以标点结束，也会继续合并相邻行以重建完整段落；空行仍作为段落分隔符，编号列表和项目符号列表会保留。

版本 0.9.6 – 2026-09-09

1. 修复了启用光标跟随时部分 SAPI4 语音卡住的问题。桥接程序现在等待语音合成真正完成，同时保持光标跟随功能启用。

2. 修复了播客录制中麦克风与系统音频的同步问题，以及时间戳舍入造成的咔嗒声。这些修复也适用于分别保存两路音频。

3. SAPI5 音频描述合成现在在独立进程中运行。失败时会降低并发数重试，保留已成功合成的描述，并记住该语音可用的并发上限。

版本 0.9.5 – 2026-09-07

AI 音频描述
1. 提高了 Gemini 临时返回 `MALFORMED_RESPONSE` 且没有可用内容时的稳定性。Sonarpad 现在会对完全相同的分段最多重试三次，并在重试之间短暂等待，而不是立即中止整个音频描述流程。Gemini 的正常响应、安全拦截、令牌上限错误以及现有的检查点行为均保持不变。

2. 修复了另一种罕见的 `invalid Gemini chunk timeline` 情况：某些视频由 FFmpeg 准备的分段会报告轻微的累计时长偏差。只要时间轴本身有效，Sonarpad 就完全保持原有流程不变；只有在正常时间轴将失败且总偏差很小时，才会启用保守的重新对齐回退。较大或可疑的偏差仍会继续报错。

3. 新增可选的 Sonarpad AI 服务用于 AI 音频描述。用户仍可继续使用自己的 Gemini API 密钥，也可以选择 Sonarpad 服务并输入个人 Sonarpad 代码。代码通过 Windows DPAPI 保护。在服务模式下，准备好的视频分段会通过临时 Google 上传 URL 直接从用户电脑上传到 Google；sonarpad.com 不接收也不保存视频字节。后端仅负责授权访问、强制使用指定的 Gemini 模型/Standard 层级和使用限制、记录用量，并返回 Gemini 响应。

4. 改进了已保存音频描述项目的编辑。现在可以连续修改多条描述，而不必立即逐条应用。修改会保留在内存中；按“应用文本”后，Sonarpad 会根据已保存的时间范围逐条检查所有修改过的描述，然后一次性保存全部有效修改。如果某条描述超出范围，焦点会返回该描述，同时保留其他尚未应用的修改。

5. 在“创建音频描述”之前新增“调整语音”。新窗口可选择语音合成引擎、语音、速度和音量，并提供“测试语音”。按 OK 后，焦点会准确返回“调整语音”，所选参数将应用于即将创建的音频描述。如果从未使用此窗口，语音合成行为与以前完全相同。


6. 扩展了已保存音频描述项目中的语音控制。在“修改项目”中，除了语音引擎和声音外，现在还可以调整速度、音量并使用“测试语音”。应用语音参数后，Sonarpad 会使用新的合成参数重新检查所有现有描述，并且只有在所有描述仍能放入各自已保存的时间范围时才重新生成 MP3。无对白区间、Visual Evidence 时间点、Gemini 描述和项目时间分析都会直接复用，不会重新运行 AI 分析。

版本 0.9.4 – 2026-09-04

AI 音频描述
1. 修复了部分 Matroska/WebM 视频因包含异常大的内部起始时间戳而导致音频描述生成以“invalid Gemini chunk timeline”错误中止的问题。Sonarpad 现在仅在检测到这种异常时间戳模式时，才会规范化源文件和 Gemini 分段的时长；普通视频以及原本已正常工作的音频描述流程保持不变。
2. 改进了音频描述的 ducking，使最终混音更加自然。原始音轨现在会在旁白开始前平滑降低，并在描述结束后更缓慢地恢复到正常音量；当两段描述间隔很短时，背景音量会保持稳定，避免反复升高和降低。
3. 修复了部分 AVI 文件及其他 5.1/多声道音频源在最终导出音频描述时可能失败的问题：当解码以一个不完整的交错音频帧结束时，导出可能会中止。Sonarpad 现在只在必要时为最后这个不完整帧补入严格所需数量的静音采样；已经完整的音频帧以及正常的单声道、立体声或多声道文件均保持不变。


版本 0.9.3 – 2026-09-03

SAPI5 语音
1. 修复了部分本地 SAPI5 语音在启用光标移动、多语音朗读或创建有声书/MP3 时可能无法发声的问题。Sonarpad 现在使用在 Windows 上可靠工作的 SAPI5 文件合成路径，并且在合成过程中仍可取消；普通直接朗读保持不变。
2. 修复了多语音对话朗读时的光标位置。Sonarpad 为对话自动插入的语音标签现在只作为播放元数据处理，不再计入编辑器中的实际字符，因此按 F4 或 F6 后光标不会再跳到真实文本之前。单一语音朗读以及文档中明确写入的 <voice> 标签保持不变。

AI 音频描述
1. 在 Gemini API 密钥输入框之后新增“显示 API 密钥”复选框。默认情况下该选项关闭；启用后会临时显示完整密钥，便于确认密钥是否已完整粘贴。重新打开窗口时，密钥会再次自动隐藏。

播客和维基百科
1. 保存播客录音后，Sonarpad 现在会询问是否打开包含已保存文件的文件夹，与保存 YouTube/流媒体内容后的行为一致。
2. 当编辑器中已经有文本并继续导入另一篇维基百科文章时，新文章现在会追加到末尾，而不是插入到顶部。光标会定位到刚导入文章的开头。


版本 0.9.2 – 2026-09-02

AI 音频描述
1. 修复了在包含多声道音频（例如 5.1）的影片中，AI 音频描述可能在最终导出 MP3 时失败的问题。Sonarpad 现在仅在 MP3 编码需要时自动将多声道音频下混为立体声，不会改变单声道或立体声导出。
2. 当使用包含多条音轨的视频创建 AI 音频描述时，Sonarpad 现在会在处理前询问要使用哪条音轨。可访问的组合框可用方向键切换；选择“确定”会使用所选音轨开始创建音频描述，选择“取消”会关闭音频描述窗口并将焦点返回 Sonarpad 编辑器。

YouTube 和流媒体
1. 修复了一个问题：从 YouTube 播放列表或频道的第 2 页及后续页面中的视频启动 AI 音频描述时，YouTube 选择窗口可能会重新打开并抢走音频描述窗口的焦点。Sonarpad 现在会正确关闭选择窗口，不再返回之前的页面。
版本 0.9.1 – 2026-09-01

YouTube 下载
• 修复了一个问题：使用 Alt+Tab 切换到其他应用后，YouTube/流媒体下载进度窗口可能会反复回到前台。现在下载会在后台继续进行，不再抢夺焦点。
• 改进了下载进度的无障碍体验。返回进度窗口时，屏幕阅读器可以读取当前状态和百分比。对于播放列表，Sonarpad 还会报告当前项目编号、项目总数和标题。
• 修复了长时间下载和转换过程中进度窗口仍能正常响应时，watchdog 误报程序卡死的问题。
• 播放列表下载新增“格式”组合框。在视频列表中按 Tab，即可在开始批量下载前选择 MP4、MP3、M4A、OPUS、OGG、WAV 或 FLAC。
• 重新整理了流媒体保存流程。格式和质量现在在保存时选择，而不是在最初的流媒体搜索窗口中选择。“保存媒体”会打开统一的格式/质量对话框，播放列表下载中也同时提供“格式”和“质量”组合框。

AI 音频描述
• 修复了某些 MKV 视频可能无法启动 AI 音频描述的问题。Sonarpad 现在可以更可靠地处理时间戳不规则或缺失的视频。

版本 0.9.0 – 2026-08-31

AI 音频描述——重要的新功能
• 在“工具 > 多媒体”中新增“使用 AI 创建音频描述”。Sonarpad 会分析音频以寻找没有对白的空隙，使用 Gemini 生成描述，并调用 Sonarpad 中已有的语音合成引擎，同时避免在对白上方朗读。
• 改进了视频中实际发生的内容与生成描述之间的同步，并自动检查 Gemini 生成的时间标记。
• “启用扩展停顿”默认关闭。对于对白很多或可用空隙较少的内容，可以启用它，以便仍能插入较长的描述。
• Sonarpad 可以尝试识别角色并使用其姓名。角色目录可以在同一剧集的多个分集之间保留，以提高连续性。
• 项目可以保存、稍后编辑，并在无需 Gemini 重新生成全部内容的情况下再次导出。
• 如果过程被中断，Sonarpad 会保留进度并可继续音频描述。如果 Gemini 配额用尽，可以等待、更换模型或停止，而不会丢失已完成的工作。
• 窗口允许选择语言、详细程度、Gemini 模型、语音合成引擎和声音，并会记住所选首选项。
• 该模块支持 Sonarpad 的全部 17 种语言。生成期间，界面只显示进度、当前状态和“取消”；完成后可直接在内部播放器中打开 MP3。

电子书和文档
• 新增无 DRM 的 Kindle 导入，支持 MOBI、AZW 和 AZW3，文本和章节可在编辑器和文档索引中使用。
• 新增 DAISY 2.02 和 DAISY 3 支持。DAISY 有声书使用 Sonarpad 内部播放器，并遵循章节导航和播放范围。
• Kindle 和 DAISY 文件导入时不会覆盖原文件；受 DRM 保护的 Kindle 图书会被明确拒绝。
• 修复 EPUB“另存为”：选择 TXT 或其他格式时，现在会使用所选扩展名，并保持原 EPUB 与当前打开文档的关联。

RSS 和文章
• 为 RSS 文章新增多选功能，可在一次操作中删除多篇文章。
• RSS 现在支持真正的文件夹，并在 OPML 导入和导出期间保留这些文件夹，包括空文件夹。
• 可在当前文件夹中使用“上移”“下移”“移到顶部”“移到底部”和“移到指定位置”重新排序源。

无障碍、指南和界面
• Sonarpad 指南已重新整理并加入索引，同时新增完整的 AI 音频描述指南。
• 修复德语翻译中的问题，该问题可能导致“打开”“另存为”和其他文件选择对话框无法显示。

语音和语言
• 可下载的 Google TTS 目录从 104 个包增加到 156 个包，语言变体从 53 种增加到 81 种。
• 在整个界面中新增更多 Google TTS 包以及其他语言的本地化名称。

版本 0.8.4 – 2026-07-24

EPUB 文档编辑
• Sonarpad 现在不仅可以打开 EPUB 文档，还可以编辑并再次以 EPUB 格式保存，同时保留原始格式、目录、脚注、图片、样式表、元数据和内部链接。
• 对从 EPUB 打开的文档，“另存为”中可选择 EPUB。保存时只更新已修改文本，并保持图书结构完整。

有声书可靠性
• 修复一个间歇性问题：Google TTS 连续五次失败后，某个合成单元可能被静默丢弃，导致最终有声书缺少部分文本。
• Google 单元现在会持续重试，直到成功或用户取消。工作进程启动时间会错开，以减少临时的 Chrome 和文件冲突；如果存在缺失片段，Sonarpad 现在会停止，而不是保存不完整的有声书。
• Edge 有声书现在会针对临时网络、WebSocket、超时、服务限制和无效音频响应持续重试，直到成功或用户取消，包括混合语音和按时间分段。SAPI4 和 SAPI5 继续采用自适应有限恢复；如果某片段仍失败，Sonarpad 会停止而不会保存不完整的有声书。

数字图书馆导航
• LibriVox、Internet Archive 和 Project Gutenberg 的搜索结果现在像 YouTube 一样使用分页导航：“转到上一页结果”显示在顶部，“转到下一页结果”显示在底部。
• 修复 LibriVox 的焦点切换：打开图书或章节时，在下一列表或播放器出现之前，不再把 NVDA 焦点发送到主编辑器。
• 在 LibriVox 搜索和图书加载期间新增焦点保护：请求运行时，本地化加载对话框会保持在前台，防止 NVDA 焦点跑到命令提示符、Windows Terminal 或其他应用。

YouTube 播放列表下载
• 为 YouTube 播放列表新增可访问的多选命令，允许用户选择要下载的视频，同时不改变当前播放项目已有的“保存媒体”命令。
• 所选项目会按照打开播放列表时选择的格式和质量逐个下载，使用带编号的文件名保持播放列表顺序，并保存到已配置 Media 文件夹中的专用子文件夹。
• 选择窗口包含“全选”和“全部取消选择”命令，会报告已选择项目数，支持取消并保留已经完成的文件，还会报告无法下载的项目。
• 播放列表项目现在使用原生复选框：屏幕阅读器会自动读出每个标题、控件角色和选中状态，无需在可见标题中添加选择文字，也无需强制朗读。

版本 0.8.3 – 2026-07-23

深色模式
• 新增深色模式，可从“查看”菜单启用，并保存到用户首选项中。
• 深色主题会应用到编辑器、菜单、辅助窗口和主要控件，并调整文本颜色以保持可读性和无障碍体验。

德语
• 新增德语作为完整界面语言，可在“选项”中选择。
• 新闻和 RSS、拼写检查、日历以及所有引语、捐赠、指南和更新日志均已完整提供德语版本。

巴西葡萄牙语和 Google 新闻
• 新增巴西葡萄牙语作为完整界面语言，与葡萄牙语（葡萄牙）分开，并可在“选项”中选择。
• 完整界面、日历项目和引语、拼写检查、捐赠、指南及更新日志均提供巴西葡萄牙语版本。
• Google 新闻现在支持巴西本地化、巴西分类以及独立的默认巴西 RSS 源。
• 当 Feed 提供同一新闻的相关 Google 新闻来源时，它们会作为可访问的子项目显示在树中。

LibriVox
• 优化 LibriVox 搜索，以避免对服务发送过多请求和界面冻结。移除了大型目录扫描，减少了尝试次数，并使用更短的超时时间。

语音合成
• 三个或更多连续句点在朗读前会被规范化，防止某些语音说出“点 点”，或生成只有标点符号的片段。

Google 新闻相关文章
• 对每条新闻，现在会在可用时显示相关文章，也就是报道同一事件的其他文章。Sonarpad 提示存在相关文章时，只需展开主文章即可阅读。不想展开此部分的用户可以直接在主文章上按 Enter，像以前一样阅读新闻。
• 相关文章现在使用与主文章相同的已读/未读系统，包括无障碍提示、日期和时间、保存状态，并在 Feed 更新或重启 Sonarpad 后继续保留。

有声书分段提示
• 在“音频选项”中新增“每个分段开始时的提示”组合框。对于拆分为多个文件的有声书，每一段可选择无提示、书名、书名和分段编号、文件名，或文件名和分段编号。

版本 0.8.2 – 2026-07-17

数字图书馆和有声书
• 新增 Project Gutenberg，可按标题或作者搜索，并可选择语言。
• Project Gutenberg 的 EPUB 图书会下载到 Documents\Sonarpad\Documents；下载完成后，Sonarpad 会询问是否立即在编辑器中打开图书。
• 新增 Internet Archive，用于搜索和收听音频收藏，包括老式广播、演讲和现场音乐。
• 新增 LibriVox，可按标题或作者搜索有声书，并直接使用与播客相同的播放器播放章节。
• 这三个新功能都位于“工具”菜单中；启用菜单分组时，也会出现在“阅读”部分。

长音频转录
• 修复长音频文件的转录：音频现在会自动分成 15 分钟的片段，逐段转录后再合并，从而避免长录音可能出现的错误。

YouTube
• 以前只有打开 YouTube 视频并进入“播放”菜单后才能使用的一些常用操作，现在也可直接从该视频的上下文菜单使用，例如“转录当前音频”“使用 AI 创建音频描述”和“保存媒体”。
• 新增“复制链接”，也可使用 Ctrl+C，将所选 YouTube 视频、播放列表或频道的 URL 复制到剪贴板。

版本 0.8.1 – 2026-07-16

Google 文本转语音
• 修复 Google TTS 在部分 Windows 系统上的启动问题：内部浏览器服务器接受的连接会继承非阻塞 socket 模式，导致错误 10035 并阻止已下载语音发声。
• Sonarpad 现在会等待 Chrome 或 Edge WASM 引擎完全加载后再进行语音预览或使用 F5 阅读，从而避免“Chrome WASM TTS engine was not loaded”错误。
• 隐藏浏览器会禁用页面翻译和渲染器无障碍功能，因此不会再提示“翻译页面”或干扰阅读命令。
• 选择 Google 引擎时，“编辑器中的语音”面板现在会显示“管理 Google 语音...”按钮，并在管理器关闭后立即刷新已安装语音列表。
• 删除 Google 语音包时显示的依赖项警告现在已针对所有界面语言本地化。

更新体验
• 自动更新后，完成和更新日志窗口现在会在编辑器初始焦点恢复之后打开并保持在前台，而不再需要按 Tab 才出现。

PDF 文档
• 修复嵌入文本包含 NUL 字符的 PDF 文件；此前加载到编辑器时会在第一个 NUL 处被截断。
• 当 pdf-extract 返回嵌入的 NUL 时，Sonarpad 现在会使用 PDFium 重试；剩余 NUL 会在文本发送到 Windows 控件之前被移除，从而保留文档其余内容。

菜单无障碍
• 移除了运行时助记键生成：访问键现在明确写入 15 种界面翻译中，因此每次启动都保持一致。
• 检查了所有稳定的主菜单项和子菜单，包括播放、字体选择、保存图片和显示 EPUB 索引；缺失或在同级中重复的助记键直接在翻译中修正。
• 自动测试现在只验证翻译；如果助记键缺失、无效或重复就会失败，并且绝不会在运行时修改菜单标签。
• 对于非常大的菜单，如果翻译后的标签无法提供足够多不同字符，会使用 Windows 标准格式“(&1)”显示明确的数字访问键。

版本 0.8.0 – 2026-07-15

在线词典
• 在线 Wiktionary 词典新增德语。
• 德语定义和同义词现在会按照德语 Wiktionary 的结构解析，而不只是把语言加入选择列表。

SAPI5 有声书可靠性
• 当所选语音输出可靠时，SAPI5 有声书创建会保持最多 12 个并行工作进程。
• 每个生成的部分现在都会根据文件大小、预计时长以及与分配文本的保守比较进行检查。
• 缺失或可疑的部分会自动以逐步降低的并发数重新生成：12、8、6、4、2，最后为 1 个工作进程。只重复有问题的部分。
• 每个 SAPI5 语音的可靠工作进程上限会分别记住，不会拖慢能稳定使用 12 个进程的语音。
• 最终完整性检查会阻止 Sonarpad 静默接受明显短于已生成各部分的 MP3。
• 详细诊断信息写入 `sapi5_audiobook_diagnostic.log`。
• 每个 SAPI5 合成单元现在都在独立的隐藏 Sonarpad 进程中运行。如果第三方语音崩溃，只关闭该工作进程，主程序仍保持打开。
• 在同一次有声书创建中，未完成的部分会立即使用下一级较低并发数重试；已经验证的部分会保留。
• 下次启动时的恢复仍作为额外保护，仅用于主程序或计算机被中断的情况。

SAPI4 有声书工作进程
• 现在会遵守用户选择的 SAPI4 进程数，技术上限为 64；此前隐藏的 16 个进程限制已删除。
• 只有当有声书的工作单元少于请求数量时，实际进程数才会减少。
• 如果一个或多个 SAPI4 bridge 进程失败，已完成部分会保留，只对失败单元自动以逐步降低的并发数重试。
• Sonarpad 现在会检查 SAPI4 bridge 的退出状态，并拒绝空白或无效音频部分，而不是将其视为成功。

代理配置
• 在网络设置中新增独立的代理端口字段。
• 端口现在可独立于代理地址输入，会验证 1 到 65535 的范围，并能正确替换 URL 中已有的端口。

按语言和国家搜索广播
• “语言”和“国家”筛选器现在会使用 Radio Browser 目录中所有可用项目更新，而不是局限于固定列表。
• 即使 Radio Browser 使用其他文字系统、本地名称、缩写或多种语言组合提供语言名，现在也能识别，并以当前界面语言的译名显示。并非真实语言的值，如数字、流派、国家或泛化标签，会被过滤掉。
• 目录会在后台刷新；当无法连接 Radio Browser 时，备用列表仍可使用。
• Radio Browser 中翻译后变得相同的重复语言项目现在会合并为一个组合框项目，避免屏幕阅读器出现无声步骤。

重大改进：语音与光标移动同步
• 所有受支持语音引擎的语音播放与光标移动同步都得到显著改进。
• 启用“阅读时移动光标”后，Sonarpad 现在对 Microsoft Edge Neural、Google TTS、SAPI4、SAPI5 和 OneCore 使用统一的进度系统。
• 光标会更准确地跟随实际朗读的文本，句子和短语分段也更一致。
• 过早移动、延迟、不规则跳动以及不同语音引擎之间的差异都大幅减少。
• 暂停、继续、在文档中搜索或更换语音引擎后，现在能更可靠地保持正确位置。

播客录音的独立音轨
• 新增“将麦克风和系统或应用音频保存到独立文件”。
• 同时录制麦克风和另一个来源时，Sonarpad 可以创建一个仅含麦克风的文件，以及另一个包含系统音频、单个应用或所选应用的文件。
• 独立来源录音同时支持 MP3 和 WAV。
• 关闭此选项时，Sonarpad 仍会创建一个正常混音的文件。
• 独立文件更便于调整音量、降噪以及后续编辑播客、访谈和教程。

定时广播录音
• 广播录音现在可以提前安排。
• 每次录音可选择电台、日期、开始小时和分钟以及时长。
• 可设置 1 到 1,440 分钟的自定义时长。
• 录音可执行一次、每天或每周。
• 录音窗口现在更清楚地显示正在进行和已安排的录音、计划日期与时间、时长以及距开始剩余时间。
• 定时录音可使用 Windows 任务计划程序，因此即使 Sonarpad 尚未打开也能自动开始。

日历
• 新增完整的键盘可访问日历。
• 用户可以浏览前一天和后一天、快速返回今天，并查看节假日和纪念日。
• 新增“今日圣人”和“每日引语”，可阅读、朗读或复制。
• 提醒可以创建、编辑、删除、推迟并标记为完成。
• 警报可在精确时间或提前显示，并可使用 Windows 计划功能，即使 Sonarpad 已关闭也能工作。

天气
• 新增天气预报部分。
• 用户可以搜索城市，并快速重新打开最近查看的位置。
• 可查看当前天气、温度、最低和最高值、湿度、降水概率以及未来几天的预报。
• 温度可显示为摄氏度、华氏度或自动选择。

影院电影
• 新增当前上映电影和即将上映影片的部分。
• 可按标题搜索、查看剧情和上映日期并播放预告片。

Google 文本转语音
• 新增 Google TTS，用于文档阅读和有声书创建。
• 新增语音管理器，可列出语音、按语言筛选、下载并删除不再需要的语音。
• 可以调整速度、音量和音高。
• Google Natural 语音的音高由引擎直接应用，以获得更自然稳定的效果。
• 改进 Google TTS 的响应速度和可靠性，合成时间限制会根据所选朗读速度调整。
• 减少引擎无响应时不必要的等待，并改进错误与中断处理。
• 同时执行多个操作时，诊断日志更加稳定。

EPUB 目录
• Sonarpad 现在可以识别 EPUB 图书中嵌入的目录。
• 会提示目录存在，并可从“查看”菜单打开。
• 章节和子章节以层级方式显示。
• 按 Enter 会立即跳到书中所选位置。

新闻和 RSS 源
• 新闻部分新增更多搜索和整理工具。
• 新增新闻语言选择。
• 用户可以在 RSS 源中搜索并阅读所在城市的新闻。
• 可以浏览社区 RSS 源、将其加入个人集合，并提交给 Sonarpad 社区。

播客录音
• 用户可以只录制麦克风、全部系统音频、单个应用、多个所选应用，或同时录制麦克风和应用。
• 可以选择麦克风设备和音频源，分别调整各来源音量并实时监测电平。
• 新增暂停和继续、MP3 或 WAV 输出、MP3 比特率选择以及目标文件夹选择。
• 录音期间可以让计算机保持唤醒。
• 独立文件会使用不同名称，因此能立即区分麦克风轨道与系统或应用音频。

广播
• 广播部分进行了大幅重新整理。
• 可以按名称或自由文本、语言、国家、城市、音乐流派或分类搜索电台。
• 改进收藏管理，并可快速重置全部筛选器。
• 电台可提交给 Sonarpad 社区。
• 新增实时录音、“录制并播放”、录音列表以及录音删除和管理。
• 广播录音保存在主录音目录中的独立文件夹。

媒体播放
• 显著改进媒体播放器稳定性。
• 修复可能阻塞 mpv 的问题，并提高与播放器通信的可靠性。
• 改进不同媒体文件类型的打开。
• Sonarpad 现在会记住播放时使用的音量。
• 改进流媒体和录音处理。
• 修复通过 Windows 双击或“打开方式”打开的文件。

PDF 文档
• 新增 PDF 文档表单字段识别。
• Sonarpad 可以找到可填写字段，以可访问的文本形式呈现，允许编辑其值并将输入数据保存回 PDF。
• 修复朗读期间的光标位置计算，尤其是包含多字节字符或复杂结构的文档。
• 新的统一同步系统进一步改进所有语音引擎下的光标移动。

无障碍和键盘命令
• 改进整个程序中的标准编辑命令。
• 复制、剪切、粘贴、全选、撤销和重做现在都会正确发送到当前聚焦字段，包括辅助窗口和对话框。
• 修复可能导致盲文显示器无法正确更新的问题。
• 改进辅助窗口中的焦点处理。
• 修复 Wikipedia 窗口中的语言选择。
• 新增按分类分组“工具”菜单功能的选项。
• 新增可配置操作，用于快速打开日历、天气和影院电影。
• 改进更新后的更新日志显示。

有声书
• 改进在对话框或其他模态窗口打开时的有声书创建。
• 进度处理更加稳健，会忽略过时的音频更新，减少冻结、错误通知和窗口无响应。
• Google TTS 也可用于创建有声书，并支持速度、音量和音高控制。

人工智能
• 默认 Gemini 模型更新为 `gemini-3.5-flash`。

常规修复
• 修复多个 mpv 播放冻结问题。
• 修复部分音频和视频文件的打开。
• 改进发送给媒体播放器的命令。
• 修复语音播放期间的光标恢复。
• 修复辅助窗口文本字段中的快捷键。
• 改进有声书创建稳定性。
• 修复通过 Windows 从外部打开的文件。
• 改进媒体、RSS、广播和 EPUB 的整体处理。

版本 0.7.1 – 2026-05-13

新功能和改进
• 创建官方站点 sonarpad.com，作为获取最新消息、下载最新版程序、阅读访客评论以及未来收听所有 Sonarpad 播客的新入口。“帮助”菜单也新增“访问 sonarpad.com”。
• 修复文件名包含重音符号或特殊字符时启动语音转录会出错的问题。
• 从现在起，“查看”菜单中的“自动换行”和“播放时显示视频”等项目始终显示正确的启用或禁用状态。
• 改进 YouTube 搜索，可用 Esc 返回上一页或上一个界面。
• 新增预检查以确认视频是否可播放。播放也得到改进：Sonarpad 现在可播放标记为 Mix 的视频或播放列表，而此前无法播放。
• 改进自动书签管理。以前启用后再关闭“自动书签”，这些书签仍会生效；现在程序会正确忽略它们，直到再次启用该选项。播放到媒体文件末尾时，书签也会自动删除。
• 改进启用对话时的标签处理。Sonarpad 现在可以正确同时处理两项功能，即使启用对话选项也能插入标签。
• 改进语音设置，明确分离各个引擎，使调整更精确。语音配置文件现在分别正确保存 Edge、SAPI5 和 SAPI4 的设置。
• 新增用于插入停顿的标签，可从选项或编辑器按 Tab 进入语音面板后使用。可选 250 毫秒、500 毫秒、1 秒、2 秒或自定义时长。
• 修复播放 YouTube 视频并启动转录时的行为。现在使用 Alt+Tab 返回后，焦点会正确落在活动转录的“取消”按钮上。
• 转录完成后现在会自动保存。
• 改进 Wikipedia 导入。可选择只阅读一个章节并在文章中按 Esc 返回搜索，也可导入整篇文章，还可选择使用的 Wikipedia 语言。
• 新增全球广播部分，可按国家、语言和流派搜索电台。也可将本地电台加入 Sonarpad 数据库，让其他用户收听，并可加入收藏。
• 新增路线部分，可选择步行、骑车、驾车或轮椅方式计算路线。可选择最短或最快路线以及是否显示经过的市镇。导入路线后还可通过“文件 > 保存图片”保存可视地图。
• “文件”菜单新增“打印”。Sonarpad 使用自身系统打印 TXT；对于 DOCX、PDF 等其他文件则使用关联程序，以尽量保留原始版式。
• 每个文档新增翻译服务，可从编辑器上下文菜单使用。无需 API 密钥即可免费使用 DeepL 和 Google Translate；输入 Gemini API 密钥后也可使用 Gemini 翻译。
• 翻译菜单可选择目标语言，并会自动重新排序：如果依次选择英语、法语和意大利语，这三项会显示在语言菜单顶部。
• 输入 Gemini API 密钥后，还可在上下文菜单中使用“总结文本”，对任何文章生成摘要。
• “播放”菜单新增在播放媒体时可见的“拆分当前媒体”。支持 MP3、MP4 等格式，可按分段数量或每段时长拆分。

版本 0.7.0 – 2026-04-25

新增内容
• 新增 mpv 播放器用于流媒体播放。YouTube 和受支持网站的视频现在可即时播放；如用户选择保留，则仍像以前一样下载。转录流媒体时，会先下载再转录。mpv 也用于打开本地视频和处理字幕，提高了对许多此前未完全支持格式的兼容性。
• 改进播客的系统音频录制：现在可选择全部系统音频、单个应用或同时多个应用。此选项集成到正常录音中，因此麦克风仍可单独启用或禁用。
• 新增印地语。翻译了界面，并加入 RSS 源、更新日志和 Sonarpad 指南。
• “编辑器”选项卡新增选项：使用上/下箭头时始终把光标移到行首。
• “转换音频”菜单新增转换为 M4B。

修复
• 修复 `F10`，使其在文本阅读期间再次切换到下一个收藏语音。
• 播客录音进行时，关闭其他文档不再同时关闭正在进行的录音。
• 从“播放流媒体音频...”打开 YouTube 评论时，Sonarpad 起初只加载前 50 条顶级评论，并始终包含这些评论的所有回复；最后增加一个项目，可按需加载全部评论。
• 文本文档和媒体文件中的书签现在都按位置顺序显示和处理，而不是按创建顺序。同一位置已有书签时不再重复添加。
• “书签”菜单新增自动书签管理选项。播放本地或流媒体文件并关闭时，Sonarpad 会自动按到达位置设置书签，下次打开从该位置继续。文本文件同样会记住光标位置；如果启动过阅读，则保存最后读到的句子并从那里继续。
• “查看”菜单新增显示本地或流媒体视频画面的选项。视频会显示在放大窗口中，所有控件默认隐藏，只有按 Alt 或把鼠标移到窗口顶部时才显示，方便低视力用户使用。

版本 0.6.9 – 2026-04-08

修复
• 改进“在文件中查找”：打开“浏览文件夹”时焦点直接进入文件夹列表；按 Enter 打开结果不再破坏键盘命令；Esc 返回此前选中的结果；使用 Alt+Tab 返回时，焦点会进入搜索字段，若结果已打开则进入结果列表。
• F5 以前总是从开头阅读。现已修复，会从当前插入符位置开始，同时保留 `Shift+F5` 和 `Ctrl+F5` 用于上一句和下一句导航。
• 使用“转到行”后按 Esc 可能把焦点移出 Sonarpad。现在会正确返回编辑器。
• `自动换行` 选项现在会立即应用到已经打开的文档，而不是只在重新打开文件后生效。

版本 0.6.8 – 2026-04-07

新增内容
• “播放”菜单新增操作，可用 Whisper 转录任何音频或视频文件。“选项”中新增“AI 和转录”部分，可选择模型、为 NVIDIA 显卡启用可选 CUDA 支持、保留原始语言并启用或禁用时间戳。
• “播放”菜单新增 `转录当前文件夹`，可将当前已打开媒体所在文件夹中的所有受支持音频文件转录到一个合并文档中，并提供独立进度、当前文件状态和取消支持。也可用 `Alt+Shift+C` 启动。
• 新增离线语音听写，使用与音频转录相同的流程。默认按 `Ctrl+Shift+Space` 开始听写，再按一次停止；可在“选项”中自定义快捷键。从第二次启动开始速度更快，因为引擎会保留在内存中；内存少于 4 GB 的电脑会自动禁用预加载和复用。
• “编辑器”新增一个默认关闭的选项，可让 `Esc` 关闭编辑器窗口。
• 播客搜索现在默认使用 `iTunes + Spreaker`，如果两个平台找到同一播客会自动去重。
• 改进 Apple 播客浏览和搜索：播客搜索、分类浏览以及分类热门播客现在都会使用所选的播客目录国家。在“选项 > RSS / 播客”中可保持 `自动` 使用系统国家，也可手动选择其他国家。
• 提高 Apple 播客分类的结果上限。首次打开仍加载前 50 个结果；选择 `加载更多结果` 后，Sonarpad 最多加载 200 个结果（Apple 上限），并可顺畅浏览后续页面。
• Sonarpad 现在也提供 Mac 版本，包含部分功能。项目链接：https://github.com/Ambro86/Sonarpad-Mac

改进
• 播客目录新增 50 多个可选国家，用户可以使用更多国家和地区的目录。
• “播放流媒体音频...”现在也可根据任意文本查询搜索 YouTube，或接受 YouTube 频道/播放列表链接并显示结果。
• 改进“播放流媒体音频...”的结果显示：YouTube 项目现在更清晰地包含标题、时长、频道和观看次数。
• “播放流媒体音频...”现在支持 YouTube 评论：可从上下文菜单打开评论、阅读回复，并用右方向键展开评论线程。
• “播放流媒体音频...”新增 YouTube 频道和播放列表收藏：可从结果的上下文菜单添加；在 YouTube URL/查询字段之后按 Tab 即可到达“收藏”列表并直接打开；也可从同一列表的上下文菜单删除。YouTube 搜索结果中的上下文菜单仅对频道和播放列表提供。
• 当流媒体网站要求登录时，“播放流媒体音频...”现在可以请求凭据。用户可输入并为该网站保存，之后可在“选项 > 音频”管理已保存凭据。
• 改进“播放流媒体音频...”期间的焦点处理，使下载和转换时进度窗口更稳定。
• “语音”菜单新增两个阅读导航操作：`上一句` 和 `下一句`，快捷键可自定义。
• `使用解释器执行文件` 的默认快捷键改为 `Ctrl+Shift+F5`，因此 `Shift+F5` 默认可用于 `上一句`。
• “选项 > 语音”新增语音配置文件，可添加、重命名和删除。
• “选项 > 音频”扩展播放回退间隔，新增从 1 秒到 2 小时的更多值。
• 新增俄语翻译，感谢 Dmitriy。
• “选项 > 音频”新增音频书分段命名格式：`标题 + 编号`、`仅编号` 或 `编号 + 标题`。
• RSS 新增收藏文章：可从文章上下文菜单将项目加入专用“收藏”源。
• “收藏”RSS 源可以删除；当再次收藏文章时会自动重新创建。
• 新增 RSS 上下移动快捷键：`Ctrl+Shift+上方向键` 和 `Ctrl+Shift+下方向键`。
• RSS 窗口新增内置文章预览，可直接查看文章文本，并在用编辑器打开全文前通过 Tab 快速到达。
• 当还有更多内容时，RSS 源末尾会出现明确的“加载更多新闻”；按 Enter 加载下一批，并把焦点移动到第一个新加载的文章。
• 在语音替换词典中添加或编辑替换时，现在有“匹配大小写”复选框，可让每条替换分别选择是否区分大小写。
错误修复
• “播放流媒体音频...”现在遵守“选项”中已有的播客缓存上限，同一上限也用于音频描述播放。
• 修复 Wikipedia 导入，页面中的引用块现在可正确导入。
• 改进 WordPress 页面解析器，修复列表项和部分章节标题可能遗漏的问题。
• “转到行”现在会用当前行号预填字段。
• 修复播客和 RSS 的 OPML 导出，导出的文件现在可被 iTunes 接受。
• 为 RSS 和播客 OPML 正确导入/导出新增本地化确认消息。
• 修复“播放流媒体音频...”中输入搜索内容后选择 YouTube 频道时，程序可能看似卡住而不是打开该频道视频的问题。
• 修复打开文件列表错误显示在“帮助”菜单而非“窗口”菜单的问题。
• 修复一个流媒体边缘情况：当下载文件已经符合目标格式时，播放可能已经开始，但“正在下载流”对话框仍保持打开。
• 修复 MP3 流转换：如果流本身已是 MP3，而用户明确选择 MP3 比特率（如 128 kbps），Sonarpad 现在会重新编码到所选比特率，而不是跳过转换。
• 修复媒体转录文档：关闭时现在会询问是否保存，建议文件名会使用被转录媒体的文件名，而不是文本第一行。
• 修复 `Alt+Shift+L`：现在可在播放时正确打开章节列表。
• 修复 `Alt+Shift+T`：现在会正确启动“转录当前音频”，而不是打开“工具”菜单。
• 修复“播放”菜单中的停止处理：按 `.` 现在与“停止”相同，只停止当前轨道，不再同时退出播放器/节目。
• 修复从“最近文件”打开媒体时“播放”菜单中的保存项：如果文件来自 Sonarpad 本地缓存，现在也会正确显示本地化保存操作。
• 如果开始转录时音频正在播放，Sonarpad 现在会先自动暂停音频。
• 修复从 Wikipedia 导入文章成功但屏幕上没有显示文章文本的问题。
• 新增从本地媒体文件读取内嵌播客章节（例如 MP3 章节元数据）：当 feed/URL 没有章节时，Sonarpad 会在后台从下载文件加载，因此播放可立即开始，章节准备好后立即应用。
• 修复把已下载播客节目作为普通本地媒体文件打开时的章节加载：现在也可使用内嵌章节，不再仅限从“播客”窗口开始播放。
• 修复 SAPI4 和 SAPI5 的 MP3 音频书最终处理，长时间导出后输出会正确收尾，避免不完整或脆弱文件。
• 所有音频书创建模式新增明确的最终处理进度条：创建阶段结束后，Sonarpad 会播报并显示独立的最终处理阶段和可见进度。
• 修复对话语音调节：合成期间第一和第二个对话语音现在都会正确应用速度、音调和音量。
• 改进日语 `.txt` 编码检测：对乱码情况增加安全的 Shift_JIS/CP932 回退，同时保留现有 UTF、变音符号和中文处理。
• 内部安全重构：可行处改为安全实现，并显著减少 unsafe 代码行数。

版本 0.6.7 – 2026-03-02
改进
• 程序现在能在大型文件中高效执行大量“全部替换”。
• 更新波兰语翻译，感谢 DJ Graco。
• 新增立陶宛语翻译。
• 新增中文翻译。
• 今后会在项目 Releases 中频繁发布测试版，让用户可在下一个稳定版前试用新变化。
• 新增 `Ctrl+.` 输入省略号字符（…）。
• 改进播客章节支持：章节导航更加可靠，包括章节未内嵌到 MP3 的直播/流式节目；可用 feed/URL 中的章节元数据作为回退。新增 `Ctrl+Alt+PageUp` 上一章和 `Ctrl+Alt+PageDown` 下一章。
• 重新组织 `Documents\\Sonarpad` 下的输出目录：文件分别保存到 `audiobooks`、`documents`、`recordings` 和 `media` 子文件夹，并自动从旧路径迁移。
• 改进超大文本文件（包括 60 MB）的支持：打开和逐行导航更顺畅，尤其适合屏幕阅读器。
• 更新所有语言的指南和程序本地化资源，包括捐赠文本和 NSIS 安装程序翻译（新增简体中文和立陶宛语安装字符串，并完成乌克兰语安装翻译）。
• 在线功能新增全局网络代理支持（HTTP/HTTPS、SOCKS5/SOCKS5H）；保存“选项”时会验证代理，无效代理会提示并自动移除。
• “工具”新增“播放流媒体音频...”：可粘贴 URL（YouTube 或直接媒体链接），选择输出格式与质量/比特率配置（MP3、MP4 可使用原始质量/比特率），并直接在 Sonarpad 音频播放器中播放。
• 支持系统媒体 Play/Pause 键（耳机/键盘）：现在可控制媒体播放以及文本阅读的暂停/继续；两者同时活动时媒体优先。
• “文件 > 最近文件”新增“清除最近文件”，可快速清空最近文档列表。
• 扩展“转换音频”和播客录制中的音频比特率：新增 64/96 kbps，MP3 最高扩展到 320 kbps，并统一验证和编码处理。
• 音频书按时间分割选项扩展到 60 分钟。
• 改进音频书按份数分割：现在可手动输入 1 到 100 的份数，并进行验证。
• “视图 > 只读模式”新增选项，可防止意外编辑，同时保持文档完全可读、可导航。
• 程序更新时新增无障碍进度条，让屏幕阅读器实时跟踪下载进度。
• 主窗口新增安静的状态栏，显示字符、单词及行/列，例如“字符（含空格）：11 | 单词：2 | 行 1，列 12”，不会干扰 NVDA 焦点。
• “视图”菜单新增“自动换行”开关，无需打开“选项”即可快速切换。
• “编辑 > 文本”新增增加/减少缩进操作，快捷键为 Ctrl+Shift+. 和 Ctrl+Shift+,；因为启用“在编辑器中显示语音”时 Tab 用于语音面板导航。
• RSS 文章和播客节目新增本地化日期/时间，格式随当前界面语言调整。
• RSS 上下文菜单新增通过电子邮件分享所选文章。
• “选项 > RSS 和播客”新增细分删除确认：RSS（源/文章/两者/无）和播客（播客/节目/两者/无）。
• 新增可配置的 RSS Ctrl+C 快速复制（选项 > RSS 和播客）：可复制标题、URL、文章内容或全部。
• 统一 RSS 源创建：“添加源”现在同时接受直接 feed URL 和关键词（自动生成 Google News RSS），不再需要单独的关键词搜索操作。
• 按 Ctrl+A 现在会播报完成，给屏幕阅读器更明确的反馈。
• “编辑”菜单新增 Shift+F3“查找上一个”，与 F3“查找下一个”配套。
• 改进替换反馈消息，使单数/复数形式更正确。
• 词典窗口新增查询语言选择，默认“自动”（界面语言），也可手动覆盖。
• “选项”新增“快捷键”选项卡，可自定义按键绑定，并在快捷键已被其他操作占用时检测冲突并警告。
• 新增初步命令行参数支持：`-h`/`--help` 显示用法，`--version` 显示程序版本。
• 改进手动速度和音调调节的清晰度：手动字段使用以 100 为中心的刻度，100 代表正常值。
• 改进“选项 > 语音”和编辑器语音面板中的 Microsoft 语音选择：增加本地化语言组合框来按语言筛选；“仅多语言语音”仍使用单一未分组列表，启用时隐藏语言组合框。
• “选项 > 语音”新增完整 Tab 导航的对话语音配置，使用与主界面相同的语音模型（引擎、Edge 语言筛选、语音及带标签的速度/音调/音量）；还可配置第二个对话语音进行交替。规则保存在 `.ini` 中，因此不会修改文档文本。
• 改进“撤销”标签：“编辑 > 撤销”现在显示将撤销的操作，例如文本编辑、引用/取消引用行或插入语音标签；没有可撤销操作时仍禁用。
错误修复
• 修复 RTF 打开：`.rtf` 文档现在解析为普通可读文本，而不是显示原始 RTF 标记（如 `{\\rtf1...}`）。
• 修复 GB18030/GBK 编码中文文本文件：Sonarpad 现在可正确检测和解码，避免乱码。
• 改进带章节元数据/标记的 M4B 音频书创建，并修复生成 M4B 的“花栗鼠音”问题（音调/速度过高）。
• 修复音频书保存对话框的比特率界面：移除硬编码意大利语标签，并在可选比特率中加入 64 kbps。
• 修复“全部保存”(Ctrl+Shift+S)：现在可靠识别所有已修改文档，包括未保存/新建标签页，并逐一正确保存或在需要时打开“另存为”。
• 修复 Google News RSS 项目顺序：有日期时按发布时间降序显示，最新文章优先。
• 修复词典窗口中的 NVDA 标签关联：搜索字段和语言组合框现在会播报正确标签。
• 修复 RSS/播客“属性”窗口键盘操作：Tab/Shift+Tab 可到达 OK，Enter 激活 OK，Esc 安全关闭，并正确把焦点返回 RSS/播客列表。
• 修复 RSS/播客撤销历史：Ctrl+Z 现在支持删除文章/节目和源的多级撤销，不再仅限最后一次操作。
• 改进 RSS/播客删除反馈，提供明确状态播报（RSS 已删除、RSS 文章已删除、播客节目已删除）。
• 改进 RSS/播客删除/撤销后的焦点：需要时可靠聚焦第一个 RSS 源，并避免延迟重新选择期间屏幕阅读器重复播报。

版本 0.6.6 – 2026-02-13
改进
• “编辑”菜单新增“为 TTS 自动格式化”，可快速为朗读准备文本（移除 Markdown/引用并重新整理换行）。
• 改进语音标签插入：选中文本时，现在能正确应用到单行和多行选择。
• “音频”设置新增默认音频书保存文件夹选项（默认：Documents\\Sonarpad Audiobooks）。
• 音频书保存对话框在启用分割时新增默认开启的选项，可为分割部分建立专用子文件夹，方便整理。
• 音频书导出现在对 Edge、SAPI5、SAPI4 语音以用户选择的比特率保存立体声 MP3。
• 通过 bridge 新增 32 位 SAPI5 语音支持，因此仅存在于 32 位引擎的语音也可在 Sonarpad 使用。
• 语音功能重新组织到“语音和音频”专用菜单，并新增/明确“转换音频”，用于把任何支持的媒体转换为 MP3、AAC、OGG、Opus、FLAC、WAV、AIFF。
• 新增删除单个 RSS 文章和播客节目（Delete + 带确认的上下文菜单），无需删除整个 RSS/播客源；并可撤销最近一次单项或整个源的删除。
• RSS 窗口新增导出 RSS 源到 OPML，可方便保存和重新导入当前源。
• RSS 窗口新增“按关键词搜索 RSS”：输入关键词会自动生成 Google News RSS URL，并打开已预填的添加源对话框，一步创建关键词源。
• 新增塞尔维亚语翻译，感谢 Mila Kuran。
• 新增乌克兰语翻译，感谢 Ivan Shtefuriak。
• 新增多媒体多文件打开：选择/打开多个媒体文件时建立播放队列，不再替换当前文件。
• 播放时新增可变跳转：基本跳转为 1 分钟时，左/右方向键跳 60 秒，Shift+左/右跳 20 秒，Ctrl+左/右跳 3 分钟。
• 播放器新增上一轨/下一轨快捷键：Ctrl+PageUp、Ctrl+PageDown。
• 新增“重置音量”，并把“重置速度”“重置音调”等集中到“播放”中的“重置”子菜单。
• 改进安装程序：setup.exe 可选择关联所有支持文件类型或手动选择扩展名；MSI 在功能树中为每个扩展名提供关联选项（仍默认全部启用）。
• 新增“窗口”菜单和“打开的文档...”，可快速切换到任意当前已打开文件。
• 更新“视图 > 字体”：旧选择器替换为常用字体快速子菜单（Arial、Calibri、Consolas、Segoe UI、Tahoma、Verdana、Times New Roman、Georgia），并保留当前字号。
• 改进 RSS/播客播报，采用双状态模型：有更新的源节点播报“新项目”，单个 RSS 文章/播客节目播报“未读”/“未播放”；可在“选项”中关闭。
错误修复
• 修复含内联 HTML 注释 (<!-- ... -->) 的 EPUB 文本提取：章节文本现在正确解析，不再部分或全部跳过。
• 修复西班牙语 Wiktionary 查询和词典缓存：如“agua”等条目正确加载，旧的“Word not found”缓存不再复用。
• 修复部分西班牙语 RSS 源（如 El Mundo）的文章导入编码，重音字母和“ñ”会在临时编辑器中正确保留。
• 修复中欧 ANSI 文本（如捷克语/波兰语）解码：Sonarpad 更好地区分 UTF-8 与 ANSI，并选择正确代码页（包括 Windows-1250），避免变音符乱码。
• 修复带 URL 查询参数的 RSS 源（如 `rss.aspx?c=...`）持久化：重启 Sonarpad 后可正确保存和恢复。
• 修复从资源管理器上下文菜单打开 Google Drive 指针文件 (`.gdoc`, `.gsheet`, `.gslides`)：直接读取出现“Incorrect function (os error 1)”时改用系统 shell 打开，确保文档正常打开。
• 修复旧版 Excel 2010 `.xls` 读取：旧二进制 Excel 文件现在正确检测和解码，不再显示如 `ÐÏ_à¡±...` 的乱码。
• 修复拼写检查播报流程：稍后审阅文本时会再次播报拼写错误；同一错误删除后重新输入也会再次报告。
• 修复基于行的文本操作（Ctrl+Q / Ctrl+Shift+Q、排序/反转/唯一/合并行）：Shift+下方向键选一行时不再合并或截断相邻行。
• 修复上述行操作的多行情况：只用 CR 分隔的 RichEdit 选择现在会正确规范化，所有选中行都可处理且不会切掉首字符。
• 扩展 TTS 输入对可见空白符号（␠/U+2420、␣/U+2423、␉/U+2409、␊/U+240A、␍/U+240D、␤/U+2424）的规范化，防止多语言语音重复播放段落。
• 统一 Edge TTS 文本验证/清理：规范化异常或不可见空格，压缩“...”“!!!”“???”等长标点串，并跳过只有标点的片段，防止播放循环。
• 修复 MP3/播客流的播放时间播报 (Ctrl+I)：当前时间不会超过轨道时长，位置超过末尾时自动停止。
• 扩大安装程序本地化：setup.exe 新增捷克语、波兰语、法语、塞尔维亚语；MSI 保持单一 en-US 包，避免发布混乱。
• 修复卸载时清理上下文菜单：“使用 Sonarpad 打开”现在在旧注册表场景中也能可靠移除。
• 修复 SAPI5 暂停/继续可靠性：F4 正确暂停，继续时回到预期位置而非从头开始。
• 修复媒体“暂停 + 跳转 + 继续”：暂停后用左/右方向键移动，再按空格会从当前位置可靠继续，不再停止或重头播放。

版本 0.6.5 – 2026-02-07
改进
• 改进西班牙语翻译，感谢 Arturo Fernandez Rivas。
• 新增按章节分割 EPUB 音频书。
• RSS 导入现在使用专用临时标签页（标题已本地化）；“另存为”可将其转换为普通文档。
• 屏幕阅读器消息现在也会发送给 JAWS（如可用）。
错误修复
• 从插入符处阅读 (F5) 现在精确从光标开始。过去因插入符偏移与 CRLF/UTF-16 位置不一致，可能从上方几行开始。
• 修复重绘问题：在选中文本上输入时，之前的文本可能暂时消失，直到选择移动。
• 修复 EPUB 章节解析：封面或仅图片页面不再朗读 CSS（如“padding”）或产生“Sconosciuto”标题。
• 修复 Edge TTS 对 EPUB 按时间分割时，空/过大块导致“Edge audio not sent”的问题。
• RSS 文章现在解码 HTML 实体，如 &quot;、&amp;、&lt;、&gt;。
• 保存/另存为不可覆盖格式（如 EPUB）时，现在建议原文件名，而不是文本第一行。
• 修复有新节目的播客未播报为“未播放”，并把“Unheard”改为更专业的“Unplayed”。

版本 0.6.4 – 2026-02-05
改进
• 程序更名为 Sonarpad，以突出声音和音频这一核心方向。
• 对含多个音轨的媒体（如多语言 MKV），在“播放”菜单新增音轨选择。
• 播客现在会在未听节目名称前明确显示“Unheard”前缀。
• 新增基于标签的文本语音切换。示例：
  - Microsoft 语音 (Edge)：<voice edge it-IT-IsabellaNeural>Hello</voice>
  - SAPI5 语音：<voice sapi5 Microsoft Helena Desktop>Hello</voice>
  - SAPI4 语音：<voice sapi4 #1>Hello</voice>
  - 带速度/音调/音量：<voice edge it-IT-ElsaNeural speed=-20 pitch=-5 volume=-10>Hello</voice>
• 丰富播客分类。
• 改进 PDF 阅读，自动回退到 PDFium。
• 改进文章解析器，修复部分情况下内容没有完整读取的问题。
• “播放”菜单新增重置音调。
• 上下文菜单新增从选中文本创建音频书。
• 新增按时长分割音频书，并可选择第一个文件名。
• 文章阅读中的作者标签已本地化（如“by”“di”“par”）。
• 新增缩进设置（Tab/空格及宽度）以及 Tab/Shift+Tab 对选中行增加/减少缩进。
• 修复 Markdown 清理：禁用保留项目符号时正确处理 `*` 列表符号。
• 新增使用旧名称“Novapad”的选项，可用于窗口标题和开始菜单快捷方式。
错误修复
• 修复 SAPI4 音频书可能与预期不同的问题。
• 修复跳转超过媒体末尾后从头重新播放的问题。
• “在文件中查找”：在结果上按 Enter 现在会打开到正确片段位置，Esc 返回结果。
• “选项”窗口：改进“常规、语音、编辑器、音频”标签页的视觉布局，防止控件缺失或被裁切。
• 修复改变播放速度时的书签问题。
• 修复 Podcast Index 分类显示不正确。
• 修复撇号导致阅读中断：移除独立对话阅读，改用语音标签。

版本 0.6.3 – 2026-01-30
改进
• 改进麦克风检测。
• 所有格式新增即时播放支持。
错误修复
• 修复播客分类窗口崩溃。

版本 0.6.2 – 2026-01-30
新增功能
• 新增执行文件支持 (Shift+F5)。用户可在“选项”选择解释器（如 Python）、在电脑中查找，并按 Shift+F5 执行当前脚本。HTML 文件在浏览器打开。
• 支持 Google Docs 指针文件 (.gdoc, .gsheet, .gslides)，自动用默认浏览器打开。
• 支持 M4B 音频书格式 (Apple/AAC)。
• 播客搜索结果上下文菜单新增“显示节目”，无需订阅即可浏览和播放。
• 新增“转到行”（编辑菜单或 Ctrl+J），快速跳到指定行号。
• 上下文菜单新增 RSS 源和播客排序（按字母或日期）。
• 新增越南语默认 RSS 源。
• 录制对话框新增麦克风测试，可在开始前检查电平。
• 播客节目上下文菜单新增“显示说明”。
• 通过 FFmpeg 支持更多音视频格式：mkv, avi, mov, m4v, webm, mpg, ts, wmv, flv, vob, 3gp, flac, ogg, wma, aiff。
• 新增同步字幕阅读 (srt, vtt, ass, sub, sbv, lrc, smi)，可用 NVDA 或所选语音。程序会寻找与媒体同名的字幕；不同文件名可在“播放”菜单使用“导入字幕”“移除字幕”。
• “使用 Sonarpad 打开”上下文菜单为所有新增支持的音视频格式添加文件关联。
• 所有文件新增音调调整设置。
• “常规”设置新增匿名错误报告开关；“帮助”菜单新增创建诊断 ZIP。
• 新增对话使用不同语音，适用于实时阅读和音频书创建。
• 新增播客分类浏览，可按商业、艺术、体育等类别探索。
改进
• 从资源管理器打开音视频文件现在直接进入播放器，而不是文本编辑器。
• 移除不可访问 PDF 的 OCR 询问；现在自动 OCR，提升速度和体验。
• 改进无障碍终端：NVDA 阅读会记住最后一行，保持更好连续性。
• SAPI4：音频书创建完全并行化，几乎即时；新增选择并发进程数的提示。
• SAPI4：合成期间并行把分块从 WAV 转 MP3，消除转换瓶颈。
• SAPI4：改进错误处理和临时文件自动清理。
• 查找对话框：将“Regex”改为“正则表达式”，并补充缺失的搜索选项翻译。
• M4B 音频书：改进输出；按部分/标记分割现在生成一个带正确章节元数据（标题、作者）的 M4B。
• 播放器：修复播放速度不为 1.0x 时书签与时间播报精度。
• 恢复“选项”中的 Ctrl+Tab / Ctrl+Shift+Tab 导航。
• “播放”菜单新增即时重置速度为正常 (1.0x)。
• 更新所有依赖到最新版本，提高性能和稳定性。
• FFmpeg 使用动态 DLL 加载，确保兼容且不阻塞启动。
• 更新播客下载过滤器，包含新增音视频格式。
• 禁止 Ctrl+S 保存音视频文件，防止损坏。
• 改进 YouTube 转录导入，使其更稳健。
• 改进音频书分段可靠性，确保不丢失文本。
• 安装程序现为完全多语言，根据系统语言支持意大利语、英语、西班牙语、葡萄牙语、瑞典语、越南语；不支持的系统默认英语。
• 播客分类：在分类上按 Enter 现在确认选择，等同 OK。
• 改进卡死检测，避免在错误消息、“未找到文本”等模态对话框打开时误报。
修复
• 修复启动时更新日志未打开。
• 修复从资源管理器打开不可访问 PDF 时 OCR 提示不出现。
• 修复启动后可能立即失焦或关闭窗口的问题。
• 修复正则搜索的严重问题，包括 Windows 换行下“循环搜索”和“点匹配换行”选项导致无法找到文本。
本地化
• 新增波兰语翻译。
• 新增法语翻译。
• 新增捷克语翻译，感谢 Radek Žalud 和 Jiri Holzinger。

版本 0.6.1 – 2026-01-20
修复
• 修复启用“在编辑器中显示语音”会停止播客播放的问题。
• 修复某些播客因 URL 被截断而无法通过 URL 添加。
• 修复 RSS 源功能无法再添加普通 URL。
• 修复 Wikipedia 语言选项在不同设置标签页重复显示。
• 移除发布模式下错误生成的调试文件。
改进
• 改进 Microsoft 语音支持，现在使用专用播放方式和不同 user agent。
• 新增 MP4 支持。

版本 0.6.0 – 2026-01-20
新增功能
• 新增拼写检查。通过上下文菜单可检查当前词是否正确，不正确时提供拼写建议。
• 新增通过 OPML 导入/导出播客。
• 除 iTunes 外新增 Podcast Index 搜索；用户可输入免费 API key 和 secret（只需邮箱即可生成）。
• 支持 SAPI4 语音，用于实时阅读和音频书创建。
• 不可访问 PDF 新增自动 OCR 回退：没有可提取文本时自动 OCR 识别。
• 新增 Wiktionary 词典。按 Applications 键显示定义，并在可用时显示同义词及其他语言翻译。
• 新增 Wikipedia 文章导入，包含搜索、结果选择和直接导入编辑器。
• RSS 模块新增 Shift+Enter，直接在原网站打开文章。
改进
• 程序现在始终遵循麦克风选择。
• 播客窗口中，在节目上按 Enter 会立即通过 NVDA 播报“正在加载”确认操作。
• 播客搜索结果中，按 Enter 现在订阅所选播客。
• 修复和改进 Ctrl+Shift+O 与播客 Ctrl+Shift+P 快捷键标签。
• 播放速度和音量现在保存到设置，并在所有音频文件间保持。
• 播客节目新增专用缓存文件夹。可通过“播放”菜单的“保留播客”保留节目；缓存超过用户设定大小时自动清理（选项 → 音频）。
• 使用 libcurl 模拟 Chrome 和 iPhone 配置大幅改进 RSS 文章抓取，与约 99% 网站兼容。
• RSS 文章新增已读/未读状态，并在列表中明确显示。
• “全部替换”现在报告替换次数。
• 使用 Tab 浏览播客库时新增“删除播客”按钮。
修复
• 移除“帮助”菜单中多余的“待处理更新”（更新已自动处理）。
• 修复打开 MP3 时按 Ctrl+S 会保存并损坏文件。
• 修复“批量音频书”显示为“(B)… Ctrl+Shift+B”的 UI 问题，移除多余标签。
• 修复智能引号：启用时普通引号会正确替换为智能引号。
• 修复“转到书签”会把播放速度重置为 1.0。
• 修复已下载播客节目仍重新下载而不使用缓存。
键盘快捷键
• F1 现在打开帮助指南。
• F2 检查更新。
• F7 / F8 跳到上一个/下一个拼写错误。
• F9 / F10 快速切换收藏语音。
开发改进
• 不再静默丢弃错误：所有 `let _ =` 模式已移除，错误会明确传播、记录或用合适回退处理。
• 项目现在有任何警告都会编译失败：cargo check 和 cargo clippy 必须完全通过，并收紧 lint、尽量移除 `allow`。
• 移除 strlen / wcslen 等自定义实现；字符串和 UTF-16 缓冲区长度从 Rust 所有的数据获得，不再扫描内存。
• DLL 处理清理并统一到 libloading，避免自定义加载逻辑和 PE 解析。
• 移除手写字节解析辅助函数；所有字节解析使用检查过切片上的标准 from_le_bytes / from_be_bytes。
这些变化减少不必要的 unsafe，消除潜在未定义行为，使代码更符合 Rust 习惯、更稳健、更易维护。

版本 0.5.9 - 2026-01-13
新增功能
• 上下文菜单新增 RSS 排序（上移/下移/移动到位置）并检查无效位置。
• 文章上下文菜单新增打开原网站以及分享到 WhatsApp、Facebook、X。
• 新增 Esc，从导入文章返回 RSS 列表。
• 新增播客模式：搜索、订阅、收听；调整订阅顺序；Esc 停止播放并返回列表；在节目上按 Enter 开始播放。
• 播客和 MP3 新增播放速度控制。
• 新增 Ctrl+T 跳到指定时间。
• 音量组合框后新增语音试听按钮。
• 新增正则查找替换（Notepad++ 风格）。
• 新增从 OPML 和 TXT 导入 RSS。
• 新增在文件资源管理器启用“使用 Sonarpad 打开”，包括便携版。
改进
• 改进语音速度/音调/音量选择，并遵守 TTS 最大限制。
• 多项 RSS 改进，更新时下载所有文章而不移动 NVDA 焦点。
• 改进音频播放：专用菜单、Ctrl+I 播报时间、音量最高 300%。
• 为部分功能补充缺失快捷键。
• 重新组织“编辑”菜单，新增文本清理子菜单。
• “选项”重组为标签页，支持 Ctrl+Tab / Ctrl+Shift+Tab。
• RSS 阅读器现在下载完整文章内容，与浏览器视图一致。
修复
• 修复 Markdown 清理会移除行首数字。
• 修复 AltGr+Z 触发撤销。
• 修复取消音频书录制，使其快速停止。
本地化
• 新增越南语翻译，感谢 Anh Đức Nguyễn。

版本 0.5.8 - 2026-01-10
新增功能
• 录制播客时新增麦克风和系统音频音量控制。
• 新增从网站或 RSS 源导入文章，包括每种语言最重要的源。
• 新增移除当前文件全部书签。
• 新增移除重复行和连续重复行。
• 新增关闭除当前外所有标签页/窗口。
• 所有语言的“帮助”菜单新增“捐赠”。
改进
• 改进无障碍终端以避免部分崩溃。
• 改进并修复整个应用的访问键和快捷键。
• 修复关闭音频播放窗口后播放没有停止。
• 重要操作新增确认对话框（如删除重复行、删除行尾连字符、删除当前文件所有书签）。操作不适用时不显示。
• 可在库中选择 RSS 源/网站并按 Delete 删除。
• RSS 窗口新增上下文菜单，可编辑或删除 RSS 源/网站。
• 移除“把设置移动到当前文件夹”选项；应用现在根据位置自动处理：exe 文件夹名为“sonarpad portable”或 exe 位于可移动驱动器时，设置保存到 exe 旁的 `config`；否则保存到 `%APPDATA%\\Sonarpad`，若不可写则回退到 exe 的 `config`。

版本 0.5.7 - 2026-01-05
新增功能
• 新增“批量音频书”，一次转换多个文件/文件夹。
• 支持 Markdown 文件 (.md)。
• 打开文本文件时新增编码选择。
• 无障碍终端新增让 NVDA 播报新行。
改进
• 选择 MP3 时，音频书录制现在原生保存为 MP3。
• 用户可选择窗口标题中“未保存更改”星号 (*) 的位置。
• 提高更新系统在不同场景中的稳健性。
• “编辑”菜单新增“移除连字符”，用于修复 OCR 行尾断词。

版本 0.5.6 - 2026-01-04
修复
  改进“在文件中查找”，按 Enter 会精确打开到所选片段。
改进
  支持 PPT/PPTX（以文本打开）。
  打开非文本格式时现在保存为 .txt，以避免破坏格式（PDF/DOC/DOCX/EPUB/HTML/PPT/PPTX）。
  新增从麦克风和系统音频录制播客（文件菜单，Ctrl+Shift+R）。

版本 0.5.5 – 2026-01-03
新增功能
• 新增适合大量输出和屏幕阅读器的无障碍终端 (Ctrl+Shift+P)。
• 新增把用户设置保存到当前文件夹（便携模式）。
修复
• 改进“在文件中查找”的片段，让预览保持与匹配位置对齐。

版本 0.5.4 – 2026-01-03
改进
• 修复“规范化空白”(Ctrl+Shift+Enter)。
• 支持 HTML/HTM（以文本打开）。

版本 0.5.3 – 2026-01-02
新增功能
• 新增“在文件中查找”。
• 新增文本工具：规范化空白、硬换行、移除 Markdown。
• 新增文本统计 (Alt+Y)。
• “编辑”菜单新增列表命令：
• 排序项目 (Alt+Shift+O)
• 保留唯一项目 (Alt+Shift+K)
• 反转项目 (Alt+Shift+Z)
• 新增引用/取消引用行 (Ctrl+Q / Ctrl+Shift+Q)。
本地化
• 新增西班牙语本地化。
• 新增葡萄牙语本地化。
改进
• 打开 EPUB 时，“保存”现在自动切换为“另存为”并把内容导出为 .txt，避免损坏 EPUB。

## 0.5.2 - 2026-01-01
- 新增更新日志。
- 安装时为支持的文件新增“使用 Sonarpad 打开”和文件关联选项。
- 改进消息本地化（错误、对话框、音频书导出）。
- 使用“按文本分割音频书”时新增部分选择，并有“标记必须位于行首”选项。
- 新增 YouTube 转录导入，支持语言选择、时间戳选项和改进焦点处理。

## 0.5.1 - 2025-12-31
- 自动更新带确认，改进错误处理和通知。
- 音频书导出改进（按文本分割、SAPI5/Media Foundation、高级控制）。
- TTS 改进（暂停/继续、替换词典、收藏）。
- 视图菜单和语音/收藏面板、文本颜色和大小。
- 根据系统区域设置默认语言，并改进本地化。
- CI 与 Windows 打包（产物、MSI/NSIS、缓存）。

## 0.5.0 - 2025-12-27
- 模块化重构（编辑器、文件处理、菜单、搜索）。
- Windows 构建/打包 workflow 以及 README/许可证更新。
- 修复“帮助”窗口中的 TAB 导航。

## 0.5 - 2025-12-27
- 初步版本提升。

## 0.1.0 - 2025-12-25
- 首次发布：项目结构和 README。

10. Fixed segment reanalysis timing when mapping an independently analyzed mini-film back to the original movie. Sonarpad now applies the same small chunk-duration reconciliation used by the normal full analysis to descriptions, visual-evidence timestamps, and protected dialogue intervals, preventing progressive sub-second drift near the end of a chunk from moving narration onto speech.

